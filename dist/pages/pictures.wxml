
  
  <view class="null-list _3e0874d _99f35f5" wx:if="{{pictureList.length <= 0}}">
    <view class="_3e0874d">
      暂无数据
    </view>
  </view>

  <view style="height: 10rpx" hidden="{{challengeId}}" class="_99f35f5"/>

  <view class="list _99f35f5"  wx:for="{{pictureList}}" wx:key="index" wx:if="{{pictureList.length > 0}}">
    <view class="item-title _99f35f5" hidden="{{item.title === false}}">{{item.title}}</view>
    <view class="item-content _99f35f5">
      <view class="item _99f35f5" bindtap="selectItem" wx:for="{{item.items}}" wx:key="index" data-wepy-params-a="{{item.id}}">
        <image src="{{item.small}}" class="_99f35f5"/>
        <view class="check _99f35f5" hidden="{{status === 'view'}}">
          <image src="../images/icon-check.png" hidden="{{item.checked !== true}}" class="_99f35f5"/>
          <image src="../images/icon-uncheck.png" hidden="{{item.checked === true}}" class="_99f35f5"/>
        </view>
      </view>
    </view>
  </view>
  <view class="space _99f35f5"/>
  <view class="button _99f35f5" bindtap="setStatus"  wx:if="{{status === 'view'}}" data-wepy-params-a="save">下载照片</view>
  <view class="button _99f35f5" bindtap="saveToPhoto"  wx:if="{{status === 'save'}}">保存至相册</view>

  <view class="button-back _99f35f5" bindtap="setStatus"  wx:if="{{status === 'save'}}" data-wepy-params-a="view">
    <image src="../images/icon-arrow-back.png" class="_99f35f5"/>
  </view>

  
    <view class="wetoast {{$toast$reveal ? 'wetoast_show' : ''}} _99f35f5">
        <view class="wetoast__mask"/>
        <view class="wetoast__bd" animation="{{$toast$animationData}}">
            <block  wx:if="{{$toast$img}}">
                <view class="wetoast__bd__img {{$toast$imgClassName || ''}}">
                    <image class="wetoast__bd__img_desc" src="{{$toast$img}}" mode="{{$toast$imgMode || 'scaleToFill'}}"/>
                </view>
            </block>
            <block  wx:if="{{$toast$title}}">
                <view class="wetoast__bd__title {{$toast$titleClassName || ''}}">{{$toast$title}}</view>
            </block>
        </view>
    </view>
